http:
  services:
    triplet:
      loadBalancer:
        servers:
          - url: http://triplet:8080
  routers:
    triplet:
      rule: Host(`localhost`) && PathPrefix(`/iiif`)
      service: triplet
