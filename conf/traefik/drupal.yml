http:
  services:
    drupal:
      loadBalancer:
        servers:
          - url: http://drupal:80
  routers:
    drupal:
      rule: Host(`localhost`)
      service: drupal
    drupal-localhost:
      rule: Host(`drupal.localhost`)
      service: drupal
      priority: 9000
